---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section class="bg-secondary text-secondary-content p-4 rounded-sm">
    <h2 class="text-3xl mb-3">{title}</h2>
    <slot />
</section>